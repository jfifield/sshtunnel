make tunnel errors visible somewhere (when a tunnel can't be started/opened)
make connection errors visible (when host can't be found, auth fails, etc, no feedback)
when disconnected from server, make it visible to user
